if SHIELD_COS_LEFT

    config ZMK_KEYBOARD_NAME
        default "cosmos"

    config ZMK_SPLIT_ROLE_CENTRAL
        default y

endif

if SHIELD_COS_LEFT || SHIELD_COS_RIGHT

    config ZMK_SPLIT
        default y

endif